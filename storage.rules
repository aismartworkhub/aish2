rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 공개 읽기, 인증된 사용자만 업로드/삭제
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024; // 10MB 제한
    }
  }
}
